---
import { sitio } from '../config';

// Año dinámico (no "2026" literal) — se calcula en el build.
const anio = new Date().getFullYear();

const redes = [
  { nombre: 'Instagram', href: sitio.redes?.instagram ?? '#', externo: true },
  { nombre: 'Behance', href: sitio.redes?.behance ?? '#', externo: true },
  {
    nombre: 'Email',
    href: sitio.redes?.email ? `mailto:${sitio.redes.email}` : '#',
    externo: false,
  },
];
---

<footer class="bg-tinta text-crema mt-20">
  <div class="max-w-6xl mx-auto px-6 py-16 text-center">
    <p class="font-display text-3xl text-hueso">{sitio.nombre}</p>
    <p class="mt-2 text-crema">{sitio.tagline}</p>

    <div class="mt-8 flex justify-center gap-6">
      {redes.map((r) => (
        <a
          href={r.href}
          target={r.externo ? '_blank' : undefined}
          rel={r.externo ? 'noopener noreferrer' : undefined}
          class="text-crema transition hover:text-coral"
        >
          {r.nombre}
        </a>
      ))}
    </div>

    <p class="mt-10 border-t border-tinta-suave pt-8 text-sm text-crema">
      © {anio} {sitio.nombre}. Todos los derechos reservados.
    </p>
    <p class="mt-2 text-xs text-crema">
      Sitio por <a href="https://mi-portafolio-eta-hazel.vercel.app/" target="_blank" rel="noopener noreferrer" class="text-coral hover:underline">Kervin Martínez</a>
    </p>
  </div>
</footer>
