---
// Contenedor de sección reutilizable (ancho máx + padding consistentes).
interface Props {
  id?: string;
  class?: string;
}
const { id, class: clase = '' } = Astro.props;
---

<section id={id} class:list={['max-w-6xl mx-auto px-6 py-20 sm:py-24', clase]}>
  <slot />
</section>
